<UserControl x:Class="LabelStudio.App.Controls.StatusPill"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Button x:Name="Root" Height="32" Padding="12,0" CornerRadius="16" BorderThickness="1"
          Command="{x:Bind Command, Mode=OneWay}"
          AutomationProperties.Name="{x:Bind Text, Mode=OneWay}"
          AutomationProperties.LiveSetting="Polite">
    <StackPanel Orientation="Horizontal" Spacing="8">
      <FontIcon x:Name="ToneIcon" Glyph="{x:Bind Glyph, Mode=OneWay}" FontSize="12" />
      <TextBlock Text="{x:Bind Text, Mode=OneWay}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" />
    </StackPanel>
    <VisualStateManager.VisualStateGroups>
      <VisualStateGroup x:Name="ToneStates">
        <VisualState x:Name="Neutral">
          <VisualState.Setters>
            <Setter Target="Root.Background" Value="{ThemeResource SystemFillColorSolidNeutralBackgroundBrush}" />
            <Setter Target="Root.BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Target="ToneIcon.Foreground" Value="{ThemeResource SystemFillColorNeutralBrush}" />
          </VisualState.Setters>
        </VisualState>
        <VisualState x:Name="Success">
          <VisualState.Setters>
            <Setter Target="Root.Background" Value="{ThemeResource SystemFillColorSuccessBackgroundBrush}" />
            <Setter Target="Root.BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Target="ToneIcon.Foreground" Value="{ThemeResource SystemFillColorSuccessBrush}" />
          </VisualState.Setters>
        </VisualState>
        <VisualState x:Name="Caution">
          <VisualState.Setters>
            <Setter Target="Root.Background" Value="{ThemeResource SystemFillColorCautionBackgroundBrush}" />
            <Setter Target="Root.BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Target="ToneIcon.Foreground" Value="{ThemeResource SystemFillColorCautionBrush}" />
          </VisualState.Setters>
        </VisualState>
        <VisualState x:Name="Critical">
          <VisualState.Setters>
            <Setter Target="Root.Background" Value="{ThemeResource SystemFillColorCriticalBackgroundBrush}" />
            <Setter Target="Root.BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Target="ToneIcon.Foreground" Value="{ThemeResource SystemFillColorCriticalBrush}" />
          </VisualState.Setters>
        </VisualState>
      </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>
  </Button>
</UserControl>
